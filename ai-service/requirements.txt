# Floor pins, not exact — crewai's own dependency tree (httpx, pydantic, openai
# SDK) moves fast, and exact-pinning everything around it just recreates
# resolver conflicts every time crewai ships a minor release. `pip install` in
# a fresh environment always re-resolves compatible versions.
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-dotenv>=1.0.1
httpx>=0.27.2
pyjwt[crypto]>=2.9.0
python-docx>=1.1.2
pypdf>=4.3.1
crewai>=1.15.0,<2.0.0
openai>=1.50.0
pydantic[email]>=2.9.2
python-multipart>=0.0.9
pytest>=8.3.3
